@model Notown.Models.Place

@{
    ViewData["Title"] = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Place</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Address)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Address)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Telephone)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Telephone.Number)
        </dd>
        <dt>
            Occupants
        </dt>
        <dd>
            @if (Model.Musicians.Count() != 0)
            {
                <ol>
                    @foreach (var musician in Model.Musicians)
                    {
                        <li>@Html.DisplayFor(modelItem => musician.Name)</li>
                    }
                </ol>
            }
            else
            {
                <p>No musicians currently live here!</p>
            }
        </dd>
    </dl>
    <p class="text-danger">
        Note, this will delete <strong><u>ALL</u></strong> musicians associated with this place (<u>including their songs and albums</u>) as well as the telephone number!
    </p>
    <form asp-action="Delete">
        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            <a asp-action="Index">Back to List</a>
        </div>
    </form>
</div>
